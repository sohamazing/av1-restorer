# ==============================================================================
# AURA-Net Requirements
# ==============================================================================
# This file is for the AV1 Restorer codebase (train_*, restore_*,
# degrade_*, blocks.py, loss.py, etc.) and lists only the required
# dependencies, grouped for clarity.
# ==============================================================================

# ------------------------------------------------------------------------------
# Deep Learning Framework
# ------------------------------------------------------------------------------
# NOTE: As you specified, PyTorch is best installed separately via
# https://pytorch.org/get-started/locally/ to match your hardware (CUDA/MPS).
#
# torch>=2.0.0             # Core framework, used everywhere
# torchvision>=0.15.0      # For VGG models (loss.py) and transforms (av1_dataset.py)
# torchaudio               # Not used

# ------------------------------------------------------------------------------
# Core Training & Model Dependencies
# ------------------------------------------------------------------------------
pyyaml>=6.0              # For parsing .yaml config files (train_*.py)
tqdm>=4.65.0             # Progress bars (used in most scripts)
numpy>=1.24.0            # Numerical operations (restore_av1.py)
einops>=0.6.0            # Tensor manipulation (blocks.py)
# transformers             # Not used (marked as Phase 2)
# diffusers                # Not used (marked as Phase 2)
# accelerate               # Not used (marked as Phase 2)
# timm                     # Not used



# ------------------------------------------------------------------------------
# Data Handling
# ------------------------------------------------------------------------------
pillow>=10.0.0           # Image loading (av1_dataset.py, restore_av1.py)
pillow-avif-plugin       # Enables Pillow to read .avif files (av1_dataset.py)
ffmpeg-python>=0.2.0     # For AV1 encoding (degrade_av1.py)
opencv-python            # Not used; PIL/Torchvision handle image ops
# psutil                   # Not used
# scipy                    # Not used
# scikit-learn             # Not used
# av                       # Not used; ffmpeg-python is used instead



# ------------------------------------------------------------------------------
# Loss Functions & Metrics
# ------------------------------------------------------------------------------
lpips>=0.1.4             # LPIPS perceptual loss (loss.py)
pytorch-msssim           # MS-SSIM loss (loss.py)

------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# Development & Testing
# ------------------------------------------------------------------------------
wandb>=0.15.0            # Weights & Biases logging (train_av1_conditional_restorer.py)
# tensorboard              # Not used; wandb is the active logger
# pytest>=7.4.0            # Testing framework
# black>=23.7.0            # Code formatting
# flake8>=6.0.0            # Linting
# mypy>=1.4.0              # Type checking

# ------------------------------------------------------------------------------
# Optional - Visualization
# ------------------------------------------------------------------------------
# matplotlib>=3.7.0        # Plotting
# seaborn>=0.12.0          # Statistical visualization
# piqa                     # Not used
# colorama                 # Not used
# rich                     # Not used